import ReactFlow, { Background, Controls } from 'reactflow';

// TODO: Flow diagram
// TODO: Source edge.
// TODO: Editor

const UI = () => {
  return (
    <div>
      <ReactFlow />
    </div>
  );
};

export default UI;
